Build the SixSevenDB project in debug mode.

Run: `export VCPKG_ROOT="$HOME/vcpkg" && cmake --preset default && cmake --build build/debug`

Report any build errors with the full error message and suggest fixes.
